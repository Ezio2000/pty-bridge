{
  "name": "pty-bridge-workspace",
  "private": true,
  "version": "1.0.0",
  "workspaces": ["packages/plugin"],
  "scripts": {
    "check": "node scripts/check-packages.cjs",
    "test": "npm run check && cargo test --workspace",
    "pack:plugin": "npm pack --workspace @pty-bridge/plugin",
    "stage:native": "node scripts/stage-native.cjs"
  },
  "engines": {
    "node": ">=20"
  }
}
