{
  "description": "Bind PTY ownership and finish active sessions when the host session ends",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "mcp__plugin_pty-bridge_pty__start",
        "hooks": [
          {
            "type": "command",
            "command": "node",
            "args": ["${CLAUDE_PLUGIN_ROOT}/launcher/launch.cjs", "hook", "bind"],
            "timeout": 2
          }
        ]
      }
    ],
    "SessionEnd": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "node",
            "args": ["${CLAUDE_PLUGIN_ROOT}/launcher/launch.cjs", "hook", "cleanup"],
            "timeout": 1
          }
        ]
      }
    ]
  }
}
