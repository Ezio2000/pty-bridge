{
  "mcpServers": {
    "pty": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/launcher/launch.cjs", "mcp"],
      "env": {
        "PTY_BRIDGE_PROJECT_DIR": "${CLAUDE_PROJECT_DIR}"
      }
    }
  }
}
